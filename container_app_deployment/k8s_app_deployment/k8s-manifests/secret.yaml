# Secret for database credentials
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: creative-energy
  labels:
    app: ceweb
type: Opaque
data:
  # cedbadmin (base64 encoded)
  DB_USER: Y2VkYmFkbWlu
  # cedbadmin123! (base64 encoded)
  DB_PASSWORD: Y2VkYmFkbWluMTIzIQ==

---
# Secret for Container Registry access
apiVersion: v1
kind: Secret
metadata:
  name: registry-credentials
  namespace: creative-energy
  labels:
    app: ceweb
type: kubernetes.io/dockerconfigjson
data:
  # Docker registry credentials (to be updated with actual values)
  # kubectl create secret docker-registry registry-credentials \
  #   --docker-server=<REGISTRY_URL> \
  #   --docker-username=<ACCESS_KEY> \
  #   --docker-password=<SECRET_KEY> \
  #   --docker-email=admin@example.com -n creative-energy
  .dockerconfigjson: eyJhdXRocyI6e319